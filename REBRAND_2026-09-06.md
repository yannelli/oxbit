# Project name

The project is now named **Oxbit**, replacing Zapp. The CLI is `oxbit`, workspace packages use `@oxbit/*`, and environment variables use `OXBIT_*`.

Existing local data and settings remain accessible through the compatibility behavior described in [the runtime documentation](docs/runtime.md).
