"$schema" = "schemas/schema.json"

[default]
description = "Pick Files app folders, reopen them from bookmarks, and locate the Documents directory."
permissions = [
  "allow-pick-folder",
  "allow-open-folder",
  "allow-close-folder",
  "allow-forget-folder",
  "allow-documents-path",
  "allow-runtime-credentials",
]
